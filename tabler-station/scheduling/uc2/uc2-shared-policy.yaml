apiVersion: scheduler.kalypso.io/v1alpha1
kind: SchedulingPolicy
metadata:
  name: uc2-shared-policy
spec:
  deploymentTargetSelector:
    labelSelector:
      matchLabels:
        deployment-type: shared
        use-case: uc2
  clusterTypeSelector:
    labelSelector: 
      matchLabels:
        type: shared
        factory: tabler-station
